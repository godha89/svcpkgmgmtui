import { PackageDetailsResolver } from './package-details-resolver';

describe('PackageDetailsResolver', () => {
  it('should create an instance', () => {
    expect(new PackageDetailsResolver()).toBeTruthy();
  });
});
